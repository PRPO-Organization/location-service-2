
services:
  postgis:
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U location-user -d locations" ]
    image: postgis/postgis:16-3.4
    container_name: location-service-postgis2
    restart: always
    environment:
      POSTGRES_DB: locations
      POSTGRES_USER: location-user
      POSTGRES_PASSWORD: password123
    ports:
      - "5433:5432"
    volumes:
      - ./src/main/resources/config/postgis-init.sql:/docker-entrypoint-initdb.d/init.sql

  jakarta:
    build: .
    container_name: locations-backend2
    depends_on:
      - postgis
    ports:
      - "8080:8080"
    environment:
      DB_HOST: postgis
      DB_PORT: 5432
      DB_NAME: locations
      DB_USER: location-user
      DB_PASSWORD: password123

volumes:
  pgdata:
